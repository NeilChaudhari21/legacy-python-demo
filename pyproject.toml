[project]
name = "legacy-python-demo"
version = "0.1.0"
description = "Small demo repo for testing Python version migration workflows."
requires-python = ">=3.11"
dependencies = [
    "packaging>=23.0"
]

[tool.pytest.ini_options]
testpaths = ["tests"]
